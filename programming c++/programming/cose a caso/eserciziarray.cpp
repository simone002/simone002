#include <iostream>

using namespace std;

#define r 10

int main()
{
        /* double c[8], somma=0;
        
        for(int i=0;i<r;++i)
        {
           cin >> c[i]; //inserire valori all'interno di un array
           somma+=c[i];
        }
        cout << "questa è la somma: " << somma << endl << "questa è la media: " << somma/8;
        */

     /*  int v[r];
       for(int i=0;i<r;++i)
       {
           v[i]=0;
       }

       for(int i=0;i<r;++i)
       {
         if(i==0 || i==4 || i==7)
            v[i]=1;
       }

       for(int i=0;i<r;++i)
       {
         if(v[i]==1)
           cout << "è stato trovato un 1 alla cella : " << i << endl;
       }
       */
}